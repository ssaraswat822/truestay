[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"
  # Reviews + AI calls can take a while; bump the timeout.
  [functions."surface"]
    timeout = 60

[dev]
  command = "npm run dev"
  targetPort = 5173
  port = 8888
